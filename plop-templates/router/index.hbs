import layout from '@/layout/index.vue'
export default {
  path: '/{{ name }}',
  component: layout,
  children: [{
    // 默认路由,访问'/{{ name }}'直接访问下面页面
    path: '',
    component: () => import('@/views/{{ name }}/index.vue'),
    meta: { title: '{{ title }}', icon: '' }
  }
  ]
}
